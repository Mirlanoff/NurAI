services:
  api:
    command: uvicorn nurai.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./src:/app/src
    environment:
      NURAI_ENVIRONMENT: local
      NURAI_VECTOR_STORE_BACKEND: memory
